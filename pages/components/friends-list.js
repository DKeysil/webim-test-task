import React from 'react';
import PropTypes from 'prop-types';

export const FriendsList = props => {
	const vk = props.vk;

	return (
		<b>test</b>
	);
};

FriendsList.propTypes = {vk: PropTypes.any};
